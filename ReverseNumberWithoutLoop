// Reverse 3 digit number
#include <stdio.h>

int main() {
    int x,y;
    printf("Enter a number :");
    scanf("%d",&x);
    //Sum of the digits
    y=0;
REV:
    if (x!=0) {
    y = y*10+x%10;
    x=x/10;
    goto REV;
    }
    
    printf("Result = %d",y);
    return 0;
}
